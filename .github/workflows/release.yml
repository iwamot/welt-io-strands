name: Release

on:
  push:
    tags:
      - 'v*'

permissions: {}

jobs:
  release:
    uses: iwamot/workflows/.github/workflows/release-only.yml@086f2d88815451da2cba7dc9e6e044d86c66743c # v8.2.0
    permissions:
      contents: write  # to create the GitHub Release
